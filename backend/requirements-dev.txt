# Test-only dependencies. The runtime requirements are genuinely needed here:
# the suite covers main.py (via fastapi.testclient) and llm_service.py (with
# litellm.acompletion monkeypatched), so fastapi/litellm must be importable -
# but everything still runs offline with no keys.
-r requirements.txt
pytest~=9.1.1
httpx~=0.28
